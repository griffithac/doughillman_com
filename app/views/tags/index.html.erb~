<h1>Tags</h1>

<ul>
  <% @tags.each do |tag| %>

  <li id="tag_<%= tag.id %>">
  <%= tag.tag %>
  | <%= link_to 'Edit', edit_tag_path(tag) %>
  | <%= link_to 'delete', tag, :method => 'Delete' %>
  </li>

  <% end %>
</ul>

<%= link_to 'New Tag', new_tag_path %>
